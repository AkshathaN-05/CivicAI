SUPABASE_URL=PASTE_YOUR_SUPABASE_PROJECT_URL
SUPABASE_SERVICE_KEY=PASTE_YOUR_SUPABASE_SERVICE_ROLE_KEY
GROQ_API_KEY=
ALLOWED_ORIGINS=http://localhost:3000
ENV=development
# DEMO_USER_ID: Leave EMPTY when real authenticated users are present.
# Setting this to a real user's UUID causes all unauthenticated demo/test
# reports to appear in that user's "My Reports" list.
# Only set this for local dev WITHOUT authentication enabled.
DEMO_USER_ID=