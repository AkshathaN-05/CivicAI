[pytest]
asyncio_mode = strict
testpaths = tests
markers =
    integration: marks tests as integration tests that require a live Supabase connection
                 (deselect with -m "not integration")
